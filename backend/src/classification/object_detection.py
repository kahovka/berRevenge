def detect_extra_object(file: bytes):

    # detect that something else then just standard objects
    # is present on the image
    # if present - cut object out, normalize and return
    # largest object from the cam else none

    return None
